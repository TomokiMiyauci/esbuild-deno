export const enum Message {
  InvalidJsonFormat = "Unable to parse config file JSON '{url}'",
  InvalidJsonType = "Error config file JSON '{url}' should be an object",
  FailReadConfig = "Error reading config file '{url}'",
}
